import styles from "./yogaClass.module.css";

const First = () => {
  return (
    <div
      className={`${styles.first} margin-sections main-background`}
      >
      <h1 className="main-heading">Weight Training</h1>
    </div>
  );
};

export default First;
